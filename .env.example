# ─── 必填 ───────────────────────────────────────────────
# Payload 加密金鑰（隨機字串，請自行產生一段亂數）
PAYLOAD_SECRET=請換成一段隨機長字串

# ─── 資料庫 ─────────────────────────────────────────────
# 不填 DATABASE_URI 時，預設用本機 SQLite 檔（qiyu.db），開箱即可跑。
#
# 正式上線（Vercel）時，請改用 Postgres：
# 1. 到 Supabase 建一個免費專案
# 2. Connect → 複製 "Transaction pooler" 連線字串（給 serverless 用的那條，port 6543）
# 3. 貼到下面這行（記得換掉 [YOUR-PASSWORD]）
# DATABASE_URI=postgresql://postgres.xxxx:[YOUR-PASSWORD]@aws-0-xxx.pooler.supabase.com:6543/postgres

# ─── 圖片儲存（正式上線必填，否則上傳的照片重新部署後會消失）──────
# 在 Vercel 專案 → Storage → 建立一個 Blob store，Vercel 會自動幫你填這個變數。
# 本機開發不用填，圖片會存在本機 /media 資料夾。
# BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxx

# ─── 網站網址 ───────────────────────────────────────────
# 正式上線後換成 Vercel 給你的網址，例如 https://chiyu-website.vercel.app
NEXT_PUBLIC_SERVER_URL=http://localhost:3000
